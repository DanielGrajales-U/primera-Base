document.querySelector(".menu-btn").addEventListener("click",()=>{
    //selectionamos la clase y el metodo toggle nos añadira una clase mas si no existe
    document.querySelector(".nav-menu").classList.toggle("show");
})